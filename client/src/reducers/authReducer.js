const initialState = {
  isAuthenticated: false,
  user: {}
};

// Auth reducer
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
